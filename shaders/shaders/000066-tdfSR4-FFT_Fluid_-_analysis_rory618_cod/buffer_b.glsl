// Buffer B (buffer) — FFT Fluid - analysis rory618 cod by FabriceNeyret2
// https://www.shadertoy.com/view/tdfSR4

// proceed 2nd step of block-FFTx

void mainImage( out vec4 O, vec2 I )
{
    SUM( x, x_N0, x_N1, T( x*x_N1+i, y ) );
}